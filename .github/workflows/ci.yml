name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build native audio capture module
        run: npm run build:native

      - name: Lint
        run: NX_IGNORE_UNSUPPORTED_TS_SETUP=true npx nx lint renderer

      - name: Build
        run: npm run build

      - name: Package (smoke test)
        run: npm run package
